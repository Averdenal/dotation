{{define "layout"}}
<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="utf-8">
        <title>{{.Title}}</title>
        <link rel="stylesheet" href="assets/css/style.css">
    </head>
    <body>
        <article id="content">
            {{template "content" .}}
        </article>
    </body>
</html>
{{end}}